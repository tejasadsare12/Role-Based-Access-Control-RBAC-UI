import React from 'react';

const Navbar = () => (
  <nav className="bg-gray-800 text-white p-4">
    <h1 className="text-lg font-semibold">RBAC Admin Dashboard</h1>
  </nav>
);

export default Navbar;
